import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'gdg_Angular_CrearApp',
  webDir: 'www/browser',
  server: {
    androidScheme: 'https'
  }
};

export default config;
